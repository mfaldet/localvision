# Census API key — required for production volume; ACS works without one for
# small/exploratory queries. Get a free key (instant) at:
#   https://api.census.gov/data/key_signup.html
#
# Copy this file to `.env.local` (gitignored) and paste your key below.
VITE_CENSUS_API_KEY=
